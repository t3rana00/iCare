<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.ICare" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- SplashScreen API customizations -->
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/flower</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
    </style>
</resources>
